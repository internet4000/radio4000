<article class="Playlist">
	<p>{{#link-to 'playlists'}}&larr; back{{/link-to}}</p>
	<div class="Playlist-header">
		<div class="Inner">

			<div class="Playlist-info">

				{{#link-to 'playlist' this}}
					{{#if image}}
						<img class="Playlist-avatar" {{bind-attr src=image}}>
					{{else}}
						<img class="Playlist-avatar" src="https://i1.sndcdn.com/avatars-000069254848-ak10xz-t200x200.jpg?30a2558">
					{{/if}}
				{{/link-to}}

				<h1 class="Playlist-title">{{title}}</h1>
				<p class="Playlist-description">
					{{body}}
				</p>

				{{#if canEdit}}
					<p>can edit</p>
					{{#if isEditing}}
						<form class="Form" {{action "save" on="submit"}}>
							{{input value=title placeholder="Title" autofocus='autofocus'}}
							{{!-- {{input value=slug placeholder="Slug"}} --}}
							{{!-- {{textarea value=image placeholder="Image URL"}} --}}
							{{textarea value=body placeholder="Description"}}
							<button type="submit">Save playlist</button> <button class="Btn--text" {{action 'cancel'}}>Cancel</button>
						</form>
					{{else}}
						<p><button class="Btn--edit" {{action 'edit'}}>Edit playlist</button></p>
					{{/if}}
				{{/if}}

			</div>
		</div>
	</div>

	<div class="Playlist-body">
		<div class="Inner">
			{{outlet}}
		</div>
	</div>
</article>
