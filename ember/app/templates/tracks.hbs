<section class="Tracks">
	<h2>Tracks</h2>

	<p>{{#if canEdit}}can edit{{/if}}</p>

	<form class="Form AddTrack" {{action "publishTrack" on='submit'}}>
		{{#if isAdding}}
			{{input value=trackUrl autofocus='autofocus' placeholder="YouTube or Soundcloud url"}}<br>
			{{input value=trackTitle placeholder="Title"}}<br>
			{{textarea value=trackBody placeholder="Optional description"}}<br>
			<button type='submit'>Add track</button> <button class="Btn--text" {{action 'cancel'}}>Cancel</button>
		{{else}}
			<button class="Btn--textInput" {{action 'addTrack'}}>Add a track…</button>
		{{/if}}
	</form>

	<ul class="List">
		{{#each track in model}}
			<li>{{x-track
					track=track
					playlist=controllers.playlist.model
					authController=controllers.auth
					onPublishTrack="publishTrack"}}
			</li>
		{{/each}}
	</ul>

	{{outlet}}

</section>
