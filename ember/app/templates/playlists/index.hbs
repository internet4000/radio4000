<div class="Box">
	<h1>Playlists</h1>
	{{#each playlist in arrangedContent}}
		<article class="PlaylistTeaser">
			{{#link-to "playlist" playlist}}
				{{playlist.title}}
			{{/link-to}}
		</article>
	{{/each}}
</div>
