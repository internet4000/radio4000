import { imgUrl } from 'radio4000/helpers/img-url';
import { module, test } from 'qunit';

module('Unit | Helper | img url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imgUrl([42]);
  assert.ok(result);
});
