{{#if model.favoriteChannels}}

	<section class="Section">
		<div class="Container Container--full">
			{{#list-manipulation
					 list=model.favoriteChannels
					 sortKey=sortKey
					 as |manipulatedList updateSorting|}}
				<div class="Filters">
					{{#btn-group class="Filter BtnGroup--right"}}
						<span class="BtnGroup-label">Filter radio channels by</span>
						{{link-to "Activity" "channel.favorites" (query-params sort="updated") class='Btn'}}
						{{link-to "Age" "channel.favorites" (query-params sort="created") class='Btn'}}
						{{link-to "A-Z" "channel.favorites" (query-params sort="title") class='Btn'}}
					{{/btn-group}}
				</div>

				{{#if model.favoriteChannels}}
					<div class="Grid {{if isList 'Grid--wide'}}">
						{{#each manipulatedList as |item|}}
							<div class="Grid-cell">
								{{channel-card channel=item wide=isList}}
							</div>
						{{/each}}
					</div>
			{{else}}
					<p>Tuning radios…</p>
					{{is-loading}}
				{{/if}}
			{{/list-manipulation}}
		</div>
	</section>

	{{else}}
	<div class="Container">
		<p>This radio doesn't have any favorites, yet.</p>
	</div>
{{/if}}
