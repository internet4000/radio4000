<!-- {{input type="search" value=query}} -->

{{#if results}}
	<p class="Muted">Tap a track to add it.</p>

	<ul class="List">
		{{#each results as |item|}}
			<li class="List-item" {{action 'add' item}}>
				<a href="#">{{item.title}}</a>
			</li>
		{{/each}}
	</ul>
{{/if}}

{{yield}}
