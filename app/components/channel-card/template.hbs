<a href="{{href-to 'channel' channel.slug}}" title={{unbound channel.body}}>
	{{channel-cover src=channel.coverImage.src}}
	<h3 class="ChannelCard-title">
		{{channel.title}}
		{{!--  <small>({{channel.totalTrackss}})</small> --}}
	</h3>
</a>
<div class="ChannelCard-controls">
	{{play-shuffle-btn channel=channel class="Btn--small"}}
</div>
<p class="ChannelCard-body">
	{{do-truncate channel.body 200}}
</p>
{{yield}}
