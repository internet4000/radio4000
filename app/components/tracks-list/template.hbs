<ol class="List{{if numbered ' List--numbered'}}">
	{{#each sortedItems as |item|}}
		<li class="List-item">
			{{#if hasBlock}}
				{{yield item}}
			{{else}}
				{{item.title}}
			{{/if}}
		</li>
	{{/each}}
</ol>
