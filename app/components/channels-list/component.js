import Ember from 'ember';

export default Ember.Component.extend({
	// this component doesn't do anything yet,
	// it's more a partial. But in the future it could provide
	// sorting, filtering etc.
});
