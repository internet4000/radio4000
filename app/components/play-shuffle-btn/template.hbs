{{isActive}}

{{#if isShuffled}}
	<i class="icon-shuffle"></i> {{if expanded 'Shuffle'}}
{{else}}
	{{#if nothingToPlay}}
		Sorry, this radio is empty for now…
	{{else}}
		{{x-icon i='play'}}{{if expanded ' Play'}}
	{{/if}}
{{/if}}

{{#if isLoading}}
	<span>Loading…</span>
{{/if}}

{{yield}}
