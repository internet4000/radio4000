{{isActive}}
{{#if isShuffle}}
	{{x-icon i='shuffle'}}
{{else}}
	{{#if nothingToPlay}}
		Sorry, this radio is empty for now…
	{{else}}
		{{x-icon i='play'}}
	{{/if}}
{{/if}}
{{#if isLoading}}<span>Loading…</span>{{/if}}
