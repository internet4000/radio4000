<div id="dropzone" class="Dropzone">
	<div class="Dropzone-inner">
		<label>
			{{yield}}
			{{#if gotScripts}}
				{{cloudinary-input multiple=false}}
			{{/if}}
		</label>
	</div>
</div>

{{#if isUploading}}
	{{#if showProgress}}
		<progress value="{{progressValue}}" max="100"></progress>
	{{/if}}
	<p><small>{{progressValue}}%</small></p>
{{/if}}

{{#if showPreview}}
	<figure></figure>
{{/if}}
