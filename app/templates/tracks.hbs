{{!-- <div class="BtnGroup u-alignRight">
	<h4 class="BtnGroup-label">Sort by</h4>
	<button class="Btn" {{action 'sortBy' 'created'}}>Date</button>
	<button class="Btn" {{action 'sortBy' 'title'}}>Title</button>
</div> --}}

<div class="Container Container--noPadding">
	<ul class="List">
		{{#each track in arrangedContent}}
			<li>{{x-track
					track=track
					channel=controllers.channel
					playback=controllers.playback
					currentTrackComponent=currentTrackComponent
					onPublishTrack="publishTrack"}}
			</li>
		{{/each}}
	</ul>
</div>

{{!-- Shows help for the channel owner only --}}
{{#if showHelp}}
	<div class="Container">
		<div class="Note">

			{{#if noTracks}}
				<h2>Welcome to your new channel!</h2>
				<p>To add a track, tap, you guessed it, <em>'Add track'</em> above and paste in a complete YouTube video URL.</p>
			{{/if}}

			{{#if oneTrack}}
				<p><strong>Your first track!</strong> Why don't you add some more?</p>
			{{/if}}

			{{#unless hasImage}}
				<p>You can change the image of your channel in the top left corner on this screen. If you edit your channel, you'll see all your options.</p>
			{{/unless}}
		</div>
	</div>
{{else}}
	{{!-- normal people get this --}}
	{{#unless model}}
		<div class="Container">
			<p>No tracks yet.</p>
		</div>
	{{/unless}}
{{/if}}

{{!-- by default, the "detail page" of an x-track would be rendered here,
		but we overwrite it in the track router --}}
{{outlet}}
