import Ember from 'ember';

export default Ember.View.extend({
	didInsertElement: function() {
		// var embed = '<iframe id="player" type="text/html" width="640" height="390" src="http://www.youtube.com/embed/Eu7p9i2WCHE?enablejsapi=1" frameborder="0"></iframe>';
  // 		this.$().append(embed);
  		// var window.muchplayer;
	}
});
