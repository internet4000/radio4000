/**
 * Truncate text helper
 *
 * use in .hbs file
 * {{truncate "string you want to truncate" numberOfLetters }}
 * {{truncate "abcdefghijklmnopqrstuvwxyz" 10}}
 **/

// Ember.Handlebars.helper('truncate', function(string, length) {
// 	if (string.length > length) {
// 		return string.substring(0, length - 3) + '...';
// 	} else {
// 		return string;
// 	}
// });
