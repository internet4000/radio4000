<div class="Manchet Manchet--left">
	<h1>Discover<small> — curated radios</small></h1>
</div>

<div class="Container Container--full">
	<div class="Section">

		{{#if model}}
			{{channels-grid channels=model}}
		{{else}}
			<p>Finding channels…</p>
			{{!-- <p>Oh, there are no curated channels yet. {{#link-to 'channels.new'}}Create the first!{{/link-to}}</p> --}}
		{{/if}}
	</div>

	<p>{{#link-to 'channels.all'}}See all radios &rarr;{{/link-to}}</p>
</div>
